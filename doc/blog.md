# Blogs

## get

```javascript
try {
  const payload = {
    slug: "slug",
  };
  const response = await zott.blogs.get(payload);
  console.log(response);
} catch (error) {
  console.log(error);
}
```
